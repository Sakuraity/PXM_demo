import AboutClient from '@/components/about/AboutClient'

export const metadata = {
  title: 'About Us — VapePro',
  description: 'Learn about VapePro — a leading vape manufacturer based in Shenzhen, China.',
}

export default function AboutPage() {
  return <AboutClient />
}
